import React from 'react';

import './Logout.scss';

function Logout() {
    return (
        <main className="main logout-page">
            <section className="login-side">login-side</section>
            <section className="info-side">info-side</section>
        </main>
    );
}

export default Logout;
